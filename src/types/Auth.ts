export type TAuth = {
  firstName: string;
  lastName: string;
  userIntials: string;
  accessToken: string;
  email: string;
  userId: string;
  isLoggedIn: boolean;
  quizId: string | null;
  isLoading: boolean;
};
