/// <reference types="cypress" />

import { terminalLog } from '../support/helpers';

it('It shows the selected topics', () => {
    // cy.get('View Selected Topics').click()
    cy.url().should('include', )
})